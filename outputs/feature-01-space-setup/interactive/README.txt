ManySight — feature 01 interactive deck

Open index.html directly, or run from this directory:

  python -m http.server 8080

Then open http://localhost:8080

Slide 2 contains three interactive steps:
- Scan store
- Link cameras
- Explore the static 3D store

The 3D model supports orbit, zoom, layer toggles, zone focus and object selection.
